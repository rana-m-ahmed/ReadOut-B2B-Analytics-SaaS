import{describe,expect,it}from"vitest";import{SPRING_DEFAULT,SPRING_SNAPPY}from"@/lib/motion";describe("motion system",()=>{it("keeps exact shared springs",()=>{expect(SPRING_DEFAULT).toEqual({type:"spring",stiffness:220,damping:26,mass:1});expect(SPRING_SNAPPY).toEqual({type:"spring",stiffness:320,damping:30,mass:.9})})});
