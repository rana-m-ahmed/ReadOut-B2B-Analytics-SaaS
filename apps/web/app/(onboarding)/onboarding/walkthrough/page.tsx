import Link from"next/link";import{MessageSquareText,Pin,ScanSearch}from"lucide-react";import{OnboardingShell}from"@/components/onboarding/onboarding-shell";import{Card}from"@/components/ui/card";import{Button}from"@/components/ui/button";const steps=[{icon:MessageSquareText,title:"1. Ask a question",copy:"Start with the business question already in your head."},{icon:Pin,title:"2. Pin useful charts",copy:"Save the visual answers your team should revisit."},{icon:ScanSearch,title:"3. Review insights and anomalies",copy:"Use automated signals to decide what to investigate next."}];export default function Walkthrough(){return <OnboardingShell step={3} title="Three moves. That’s the product." description="Readout stays deliberately simple so analysis never becomes its own project."><div className="grid gap-4 md:grid-cols-3">{steps.map(({icon:Icon,title,copy})=><Card className="p-6" key={title}><Icon className="text-[var(--accent)]"/><h2 className="mt-6 font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-[var(--ink-secondary)]">{copy}</p></Card>)}</div><Link href="/dashboard/overview" className="mt-6 inline-block"><Button>Complete onboarding</Button></Link></OnboardingShell>}
