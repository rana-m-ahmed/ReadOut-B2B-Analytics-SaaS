"use client";import{useState}from"react";import Link from"next/link";import{createClient}from"@/lib/supabase/client";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{FormField}from"@/components/ui/form-field";export function ForgotForm(){const[email,setEmail]=useState(""),[pending,setPending]=useState(false),[sent,setSent]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setPending(true);setError("");try{const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth/callback?next=/reset-password`});if(error)throw error;setSent(true)}catch{setError("We could not send a reset email right now. Please try again shortly.")}finally{setPending(false)}}if(sent)return <div role="status" className="rounded-xl bg-[color-mix(in_srgb,var(--success)_10%,white)] p-5"><h2 className="font-bold">Check your inbox</h2><p className="mt-2 text-sm">If an account exists for {email}, a secure reset link is on its way.</p><Link href="/login" className="mt-4 inline-block font-semibold text-[var(--accent)]">Back to sign in</Link></div>;return <form onSubmit={submit} className="grid gap-5">{error&&<p role="alert" className="text-sm text-[var(--danger)]">{error}</p>}<FormField label="Account email"><Input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></FormField><Button disabled={pending}>{pending?"Sending…":"Send reset link"}</Button><Link href="/login" className="text-center text-sm font-semibold text-[var(--accent)]">Back to sign in</Link></form>}
