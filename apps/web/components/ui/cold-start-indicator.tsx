"use client";import{useEffect,useState}from"react";import{onColdStart}from"@/lib/api/client";export function ColdStartIndicator(){const[active,setActive]=useState(false);useEffect(()=>onColdStart(setActive),[]);return active?<div role="status" className="fixed inset-x-0 top-0 z-[90] bg-[var(--warning)] px-3 pb-2 pt-[max(.5rem,env(safe-area-inset-top))] text-center text-xs font-semibold text-[var(--marketing-ink)]">Readout’s engine is waking up — this should only take a moment.</div>:null}
