import type { HTMLAttributes } from "react"; import { cn } from "@/lib/utils";
export function Card({className,...props}:HTMLAttributes<HTMLDivElement>){return <div className={cn("rounded-[var(--radius-card)] border border-[var(--hairline)] bg-[var(--surface)] shadow-[var(--shadow-float)] backdrop-blur-xl transition-[transform,border-color] duration-300 hover:border-[color-mix(in_srgb,var(--accent)_32%,transparent)]",className)} {...props}/>}
