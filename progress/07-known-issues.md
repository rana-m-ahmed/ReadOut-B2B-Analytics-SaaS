# Known Issues

- 2026-06-20: In this Windows shell environment, `pytest tests/unit/test_config.py` completes assertions but does not always terminate cleanly before the command timeout. Test results were still emitted as `3 passed`; root cause not yet investigated.
- 2026-06-20: Superseded note: repo-root `pytest apps/api/tests -v` completed cleanly with `4 passed`, so the earlier timeout behavior appears limited to the narrower `apps/api` invocation pattern in this shell.
- 2026-06-20: Migration smoke test is currently skipped locally because no `READOUT_TEST_DATABASE_URL` and no `psql`-reachable PostgreSQL/Supabase-local target are configured. This must be run against the real project database target before Phase 1 validation is considered complete.
- 2026-06-20: The referenced project-plan Database Schema section was not present in the repo, so `0001_init.sql` was reconstructed from the requested table list and explicit task constraints. Validate against the external plan source before locking schema as canonical.
- 2026-06-20: Superseded note: the previous `PGRST205` live-project schema-cache blocker is cleared; the isolated live repository round-trip now passes against the real Supabase project.
- 2026-06-20: The live repository integration test is still intermittently flaky in the full suite because DNS resolution to the Supabase host sometimes fails with `getaddrinfo failed`. The test passes in isolation against the same project, so this currently looks like an environment/network instability rather than an application bug.
- 2026-06-20: Superseded note: `cleanup_expired_anonymous_workspaces()` is now applied on the live Supabase project and the cleanup cascade scenario passes.
- 2026-06-21: Resolved. Live RLS isolation verification previously found a public-demo access gap where anonymous JWT reads of demo datasets hit a `500` because `is_public_demo_workspace()` queried `workspaces` under RLS and recursively re-triggered `public_demo_read`. `apps/api/migrations/0004_rls_fix.sql` was applied to the live project, and the real-session isolation test now passes.
- 2026-06-21: Live storage verification requires populated `apps/api/.env` Supabase credentials and access to the real `readout-datasets` bucket. The upload integration test was run against that live bucket and passed; if those credentials are unavailable in another environment, the storage test will skip by design.
- 2026-06-21: The current live `datasets` schema still does not expose `status` / `error_reason`, so `DatasetUploadService` applies processing-state updates on a best-effort basis and no-ops when PostgREST reports the columns as missing. Raw upload and artifact storage still pass end to end.
